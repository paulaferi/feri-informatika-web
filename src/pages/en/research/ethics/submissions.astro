---
import Base from "@/layouts/Base.astro";
import SectionNews from "@/components/SectionNews.astro";
import { useTranslations } from "@/i18n/translations";
import { localePath } from "@/i18n/utils";

const lang = "en";
const t = useTranslations(lang);

const sidebarLinks = [
  { label: t("research.sidebar.projects"),     href: localePath(lang, "/research/projects") },
  { label: t("research.sidebar.publications"), href: localePath(lang, "/research/publications") },
  {
    label: t("research.sidebar.ethics"),
    href: localePath(lang, "/research/ethics"),
    children: [
      { label: "Tasks and functioning of the Committee", href: localePath(lang, "/research/ethics/mandate") },
      { label: "Submission of applications and decisions", href: localePath(lang, "/research/ethics/submissions") },
    ],
  },
];
---
<Base title="Submission of Applications and Decisions" lang={lang} sidebarLinks={sidebarLinks}>

  <h1 class="page-title">Submission of Applications and Decisions</h1>
  <div class="page-title-rule"></div>

  <div class="card">
    <div class="intro">
      <p>Researchers wishing to submit an application for ethical review must complete the standard form and submit it to the Committee secretariat.</p>
    </div>

    <div class="section-heading"><h3>Decisions</h3></div>
    <p style="margin-bottom:0;">
      Decisions are published in the opinions table on the
      <a href={localePath(lang, "/research/ethics")}>main Ethics Committee page</a>.
    </p>
  </div>

  <SectionNews tag="scientific" label={t("common.latestNews")} lang="en" />

</Base>